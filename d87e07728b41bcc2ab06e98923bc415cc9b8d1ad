=== DEPLOYED TO PRODUCTION ===
Date:        2026-04-14
Tag:         working-base-queue-heartbeat-20260414

=== Deploy artifacts ===
Relay binary md5: $(md5sum /home/tris/projects/maplecast-flycast/relay/target/release/maplecast-relay | cut -d' ' -f1)
Web queue.mjs md5: a90de84cea9844f4c1dcc264d870ad3d

Production paths:
  /opt/maplecast-relay                                    (active binary)
  /opt/maplecast-relay.backup-20260414-002523             (rollback binary)
  /var/www/maplecast/js/queue.mjs                         (active web)
  /var/www/maplecast-backup-20260414-002532               (rollback web)

=== Schema migration applied to production SurrealDB ===
  DEFINE FIELD last_seen_at ON queue
    TYPE datetime DEFAULT time::now() PERMISSIONS FULL;
  UPDATE queue SET last_seen_at = time::now() WHERE last_seen_at = NONE;

The migration is additive + idempotent — safe to leave in place even if
binary is rolled back. To remove (only if reverting beyond 434df0623):
  REMOVE FIELD last_seen_at ON queue;

=== Rollback to previous working base (434df0623, 2026-04-13) ===
  ssh root@66.55.128.93 'mv /opt/maplecast-relay.backup-20260414-002523 /opt/maplecast-relay && systemctl restart maplecast-relay'
  ssh root@66.55.128.93 'rm -rf /var/www/maplecast && mv /var/www/maplecast-backup-20260414-002532 /var/www/maplecast'
  git checkout working-base-slot-join-20260413

=== Smoke test verification ===
  Stale row (last_seen_at=now-60s) → swept in 15s ✓
  Live row, no heartbeat → alive at 20s, swept by 45s ✓
  Sweep interval 15s, grace 30s, browser heartbeat 10s

=== What's NOT in this commit (future work) ===
  - Collector's "overwrite occupied slot during promotion" bug (lives
    outside this repo, in /opt/maplecast-collector — source unknown)
  - The collector's stmt 1 array::len() type error (same)
  - Both are mitigated by this commit (heartbeat prevents stale rows
    from accumulating to trigger them) but not fixed at the source.
