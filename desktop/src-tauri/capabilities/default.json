{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the MapleCast desktop shell. The app polls input natively and loads the remote WebGPU client, so the page itself needs no IPC surface — keep this minimal.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default"
  ]
}
